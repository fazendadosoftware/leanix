{
  "name": "@fazendadosoftware/leanix-projects",
  "private": true,
  "workspaces": [
    "./packages/*"
  ],
  "engines": {
    "node": ">=12.0.0"
  },
  "author": {
    "name": "Paulo dos Santos",
    "email": "paulo@fazendadosoftware.com"
  },
  "license": "MIT",
  "scripts": {
    "lint": "eslint packages/*",
    "fix": "eslint packages/* --fix",
    "clean": "rimraf \"packages/**/dist\" \"**/*.tsbuildinfo\" \"**/*.tgz\" \"**/.temp\" \"**/.rollup.cache\"",
    "build": "npm run build:core && npm run build:vite-plugin && npm run build:create-lxr",
    "build:core": "npm run build -w=@fazendadosoftware/leanix-core",
    "build:vite-plugin": "npm run build -w=@fazendadosoftware/vite-plugin-lxr",
    "build:create-lxr": "npm run build -w=create-lxr",
    "dev": "npm run dev -w=@fazendadosoftware/custom-report-vite-demo",
    "test": "ava",
    "publish": "npm run clean && npm run build && npm run test && npm publish --access=public --workspaces",
    "prepare": "husky install"
  },
  "devDependencies": {
    "@types/node": "^14.11.2",
    "ava": "^3.15.0",
    "eslint": "^7.32.0",
    "eslint-config-standard-with-typescript": "^20.0.0",
    "eslint-define-config": "^1.0.9",
    "eslint-plugin-import": "^2.24.1",
    "eslint-plugin-node": "^11.1.0",
    "eslint-plugin-promise": "^5.1.0",
    "husky": "^7.0.0"
  },
  "gitHooks": {
    "pre-commit": "npm run build && npm run test",
    "commit-msg": "node scripts/verifyCommit.js"
  },
  "ava": {
    "files": [
      "packages/**/*.spec.ts"
    ],
    "extensions": [
      "ts"
    ],
    "require": [
      "ts-node/register"
    ]
  }
}
