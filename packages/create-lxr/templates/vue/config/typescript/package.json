{
  "scripts": {
    "build": "run-p type-check build-only",
    "build-only": "vite build --mode development",
    "type-check": "vue-tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^18.0.0",
    "npm-run-all": "^4.0.0",
    "typescript": "^5.0.0",
    "vue-tsc": "^1.0.0"
  }
}
